# ===== Secrets / local config（含真实服务器 IP、SSH key 路径、API Key，禁止入库）=====
config.json

# ===== Build artifacts =====
dist/
dist_new/
# 换包时的旧版本备份（mv dist dist_prev_<版本号>），确认新版可用后再删；绝不能入库（381MB）
dist_prev_*/
*.exe
build_*.log

# ===== Windows 保留设备名 =====
# git-bash 下误写 `2>nul` 会生成同名字节文件，且因是保留设备名而无法删除，
# 这里直接忽略，避免污染 git status。
nul
con
aux
prn

# ===== Node =====
node_modules/

# ===== Python =====
__pycache__/
*.pyc
.venv/
venv/
backend/build/
backend/build_venv/

# ===== Sync output（运行时生成）=====
backend/data/

# ===== OS =====
.DS_Store
Thumbs.db
