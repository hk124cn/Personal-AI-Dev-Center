# 2026-09-13 定时同步失败调查（SSH 服务器不可达 + LLM 全属性失败）

## 用户报告
「今天自动更新失败」。对应定时任务 `sch_1787579379861`「每周日12点同步所有」，2026-09-13 12:00:16 → 12:21:36（21.3 分钟），total 15 / success 9 / failed 6 / **llm_analyzed 仅 1**。调度器本身 `last_status=ok`、`next_run=2026-09-20`。

## 证据来源
- 实时数据在 `%APPDATA%\Personal AI Dev Center\data\latest.json` 与 `config.json`。
- `dist/app_stdout.log` / `app_stderr.log` 均为 **0 字节**（2026-08-17 构建残留，非运行日志）。
- 沙箱**无法访问** `token.sensenova.cn`（代理 502 / 直连超时），真实 API 只能用户真机测。

## 结论 1：SSH 失败 100% 集中在「腾讯云_上海」(124.xx.xx.xx)
| 服务器 | 失败 |
|---|---|
| 阿里云_成都 47.xx.xx.xx | 0/2 |
| 腾讯云轻量_首尔 43.xx.xx.xx | 0/4 |
| **腾讯云_上海 124.xx.xx.xx** | **6/8** |
| GCP 小鸡 35.xx.xx.xx | 0/1 |

- 4 个项目（mydiary / auto-claw.cn / 文件管理系统 / 拼音学习乐园）`duration` 精确 54.02~54.05s ⇒ 对上代码 `_SSH_RETRY_MAX=3` + `client.connect(timeout=15)` + 退避 `3*attempt`(3s,6s) ⇒ **15+3+15+6+15=54s**，即**三次连接全部超时**（TCP 都连不上 22 端口）。
- 2 个项目（Blog Hub / mydoctor）`No existing session` ⇒ 连上了但**会话中途掉线**（paramiko 传输层已死）。
- 定性：**该服务器/其网络当时不可达或不稳定，非程序 bug**。其余 3 台服务器 8 个项目全成功。

## 结论 2：LLM `api_failed` 是全属性问题，当前无法定因
- 8 个 `api_failed` 横跨所有 4 台服务器 ⇒ 与服务器无关，属 LLM 侧。
- 9 个有内容的项目里只有「小孩涂鸦」分析成功（1/9）。
- **查不到原因的原因**：① 运行构建是 09-06 修复**之前**的版本（`llm_error` 全为 None 可证，dist exe 为 2026-08-30）；② **backend 日志只存 Electron 内存 logBuffer（5000行/上限），从不落盘**，事后无法回溯。

## 本轮改动（排查能力补强）
1. `electron/main.js`：新增**日志落盘** — `<userData>/logs/backend-YYYY-MM-DD.log`，每行带 ISO 时间戳+级别，保留 14 天自动清理；`addLog` 同步写文件；启动/退出打会话标记；`closeLogFile()` 挂到 quit/exit。落盘失败不影响主流程。
2. `backend/llm_analyzer.py`：新增 `LLM_HTTP_TIMEOUT`（默认 **120s**，可用 `DEV_CENTER_LLM_TIMEOUT` 覆盖），替换原来 4 处 `timeout=30`。理由：商汤 6.8 属推理型模型，30s 偏紧易误判超时→api_failed。
3. （09-06 已改但**尚未部署**）`llm_error` 错误详情捕获，下次运行即可看到 api_failed 真实原因。

## 验证
- `py_compile`（llm_analyzer/sync/app）通过；`node --check electron/main.js` 通过。
- 超时常量：`timeout=LLM_HTTP_TIMEOUT` ×4，残留 `timeout=30` = 0。
- 日志逻辑单测（临时目录）：落盘成功、多行逐行带时间戳、30 天旧日志自动清理、HTTP 错误内容完整保留 ✅。

## 追加：网络归属确认（用户问「是不是我的网络问题」）
用沙箱（独立网络）对照实测 22 端口：首尔 43.xx.xx.xx **0.06s 可连**、成都 47.xx.xx.xx **0.15s**、GCP 35.xx.xx.xx **0.14s**、**上海 124.xx.xx.xx 全端口(22/80/443/8080/3389/8888/3306)全部超时**。两个独立网络都连不上同一台 ⇒ **不是用户本地网络问题**，是那台服务器不可达。用户判断「因为在日本」→ 若为安全组 IP 白名单所致则沙箱（非日本）同样被挡，结论自洽；用户决定暂时不管。

## 追加：CPU 趋势图 X 轴加「日期+时间」（用户需求）
- **根因**：`app.py` 的 `_MONITOR_HISTORY` 采样点原本**不含时间戳**（只有 cpu/mem/disk_percent），前端无从取时间。
- **后端** `app.py`：history 追加项增加 `"ts": datetime.now().isoformat(timespec="seconds")`。
- **前端** `index.html`：
  - 新增 `_monHistory`（与 `_monData` 一一对应的采样记录）、`_monHover`（悬停下标）、`monTimeAt(i,n)`（优先用 ts；**缺失时按「每分钟一点、最后一点=现在」回退估算**，兼容旧后端）、`fmtMonStamp()`（`MM-DD HH:MM`）。
  - `drawMonChart` 重写：`padB` 16→28；X 轴按宽度自适应刻度（`labelW=68`，首尾必标，末尾两刻度过近则去倒数第二个），每个刻度画竖线引导 + 日期时间标签，边界自动 left/right 对齐防裁切；**新增鼠标悬停**（竖线+白芯圆点+气泡「时间 · CPU xx%」，气泡自动避让右边界）。
  - CSS `.mon-chart` 高度 90→112px 并加 `cursor:crosshair`；副标题补充「X 轴为日期+时间，悬停可查看任意采样点」。
- **验证**：`py_compile app.py` 通过；抽取 index.html 内联 JS 交 node `--check` 退出码 0；刻度逻辑单测 320/480/600/900/1200px 分别 4/6/7/12/15 个标签、最小间距 75.6~100.5px（>68px 不叠字），n=2/3/5/10 边界正常。
- **注意**：真实时间戳需**重新部署后端**才生效；未部署时前端用「每分钟一点」回退估算（近似值）。

## 追加：监控历史与日志的固有局限
- `_MONITOR_HISTORY` 是**纯内存**，后端重启即清空 ⇒ 趋势图每次重启归零。可考虑持久化。
- 前端已有 `openLogViewer()`（内存态日志查看器）；本轮已让日志落盘，可再加「打开日志目录」按钮。

## 待办 / 建议
- **优先**：重新打包部署（含 09-06 llm_error + 本轮日志落盘 + 超时120s）→ 下周日（或手动触发）同步即可看到 api_failed 真实原因。
- 排查 `腾讯云_上海 124.xx.xx.xx` 服务器状态（SSH 22 端口连通性/负载）。
- 沙箱 shell 环境注意：`PATH` 偶发丢失（`ls: command not found`），需用**绝对路径**调用 python/node。
- 按规矩不主动推 GitHub。
