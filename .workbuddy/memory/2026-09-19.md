# 2026-09-19

## 公开仓库信息暴露事件：删库重建闭环完成 ✅

用户自己动手删掉了旧仓库、并建好同名空库，让我接手验证 + 恢复内容。全流程走完。

### 我做的验证（用户删库之后的实况）

| 探测 | 结果 | 解读 |
|---|---|---|
| repo API | 200，`id=1376930833`，`created=2026-09-19` | **新库**，不是旧库复活 |
| 旧 commit `69c8822` | **422** Git Repository is empty / No commit found | 泄露源彻底不可达 |
| 旧 raw（`<旧SHA>/.workbuddy/memory/2026-08-09.md`） | **404** | 连 CDN 缓存都失效了（之前实测 200 且能读到真实 IP） |
| 账号 `hk124cn` | 200，public_repos=15 | 排除「用户名异常导致误判 404」 |

### 推送前的自检（重要，别省）

- 一次性拉全历史 diff 扫描：`git log --all -p --no-color -- .workbuddy/memory docs`
  （**别逐 commit 读对象** —— 51 个提交 × 多文件 × 每次一个 git 进程，跑了 2 分钟直接被超时打死；
  一次性拿 diff 只要 0.2 秒，175KB）
- 结果：真实 IP **0**、密钥/私钥 **0**、真实 SSH 用户名 **0**，掩码形式 `47.xx.xx.xx` 出现 23 次 ——
  既证明干净、也证明脱敏确实进了历史。
- 用 `git push -u origin main` 推送（SSH，需 `dangerouslyDisableSandbox=true` 才能读 `~/.ssh`）。

### 推完的复检

- 远程 HEAD = 本地 HEAD = `1e461fd`；提交总数 **51**，与本地完全一致（没有夹带任何旧对象）。
- 远程 HEAD 上三个关键记忆文件：真实 IP 0、`qinwei129` 0。

### ⚠️ 判读经验：`200` 不一定是残留

旧 SHA 复检时 `f4a1f7f` 和 `d64c7cb` 返回 **200**，但这是**正确的** ——
它们是脱敏**之后**的提交，属于当前 main 的祖先，本来就该可达。
判断「是否清理干净」要看**具体哪些 SHA**：只有被 `filter-branch` 丢弃的旧对象
（`69c8822`、`87e6dda`）必须 422/404。别看到 200 就以为没清干净，白折腾一轮。

### 结论

**强推做不到的事，删库重建做到了**：GitHub 不回收未引用对象，所以改历史 + force push 之后
旧 SHA 仍可取；只有删库（换 repo id）才能让那些对象从存储上消失。
本仓库 0 star / 0 fork / 0 issue，代价极小 —— 以后遇到同类事件直接走这条路。
