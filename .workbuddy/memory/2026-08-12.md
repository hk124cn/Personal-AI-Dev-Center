# 2026-08-12

## 安全/健壮补充三项（R6 / R7 / R10）+ 系统体检文档

按用户确认，在 R1–R4 之后继续做"小而稳"的三块，并补 `docs/system-review.md`。

### R7 知识库 Markdown XSS
- `backend/app.py` 新增 `sanitize_html()`（基于 stdlib `html.parser` 的白名单消毒器，零新增依赖，兼容打包后的系统 python）。`/api/kb/render` 渲染后过一遍消毒。
- 阻断 `<script>`/`<iframe>`/`on*` 属性/`javascript:` 等；脚本/样式内部文本一并丢弃；合法 Markdown（标题/列表/表格/链接）保留。

### R6 配置/数据并发写原子化
- 新增 `atomic_write_json()`：写**唯一临时文件**（`tempfile.mkstemp`）再 `os.replace` 原子替换；对 `os.replace` 做有限重试以吸收 Windows 并发替换偶发"拒绝访问"。
- `save_config`（app.py / sync.py）与两处 `latest.json` 写入均改走它；加 `_write_lock` 串行化写操作。
- 踩坑：初版复用同名 `config.json.tmp` 导致并发写同一文件时 `PermissionError`/`WinError 32`；改为唯一临时文件名 + 重试后复测 19 项全过、无异常残留。

### R10 取消注册表加锁
- 新增 `_cancel_lock`（`threading.Lock`），`_DOWNLOAD_CANCEL`/`_UPLOAD_CANCEL` 的 get/set/pop 八处访问全部加锁。

### 测试（用户强调"做完好好测试、别引入新问题"）
- `test_security_robustness.py` 19 项全过：R7 注入清除+合法保留、R6 8 线程×50 并发写仍为合法 JSON 且无 `.tmp` 残留、R10 6 线程×200 并发无异常。
- HTTP 冒烟（另起 8766 端口、不动 live app）：`/api/kb/render` 注入 `<script>` 返回已消毒 HTML、`javascript:` 链接 href 被移除；`/api/health`/`assets`/`config/full` 均 200 且返回真实数据。
- 无头浏览器：Chromium 能加载页面（标题"Personal AI Dev Center"正常渲染），但 teardown 阶段在本机 sandbox 下偶发崩溃（环境限制，非代码问题）；页面加载+全部后端/HTTP/单测已充分覆盖回归。

### 文档与提交
- 新增 `docs/system-review.md`（R1–R10 风险表、已修/待处理、优化路线图、验证方式；纯本地不推送）。
- 本地提交（未推送）：`backend/app.py`、`backend/sync.py`、`test_security_robustness.py`、`docs/system-review.md`、记忆文件。
- 待用户确认：是否重建 exe（需先关闭正在运行的 live app，让 R6/R7/R10 在打包程序中生效）。

## 今日待确认
- 是否重建 exe 以让 R6/R7/R10 在打包 app 生效（需先关 live app）。
