# 2026-08-29 便携版双击启动不了（taskkill 自杀）+ 简化为只检测提示

## 现象（用户报告）
双击 `dist/Personal-AI-Dev-Center.exe` 启动不了；但解压目录（tmp/win-unpacked）下的 `Personal AI Dev Center.exe` 能启动。

## 根因
`electron/main.js` 旧 `clearStalePort()` 按镜像名 `taskkill /F /IM "Personal-AI-Dev-Center.exe" /T`。
便携版进程树：`Personal-AI-Dev-Center.exe`（artifactName 自解压启动器，＝本进程的**父进程**） → `Personal AI Dev Center.exe`（productName 真正的 Electron 主进程＝本进程）。
taskkill 命中**父启动器**，`/T` 顺进程树把本进程一起杀掉 → 双击无窗口；后端 exe 因在 taskkill 之后才 spawn，侥幸存活成孤儿、继续占 8765。
（代码注释原以为"taskkill /IM 不会终止调用者自身"——错误，它不杀自己，但会杀自己的父进程并 /T 顺藤杀回。）

## 用户意见
「杀进程搞得太复杂了，就启动时检查一下有没有相关进程在跑。已经在跑了，弹提示，关闭。等使用者自己去杀进程就行。」——采纳，移除全部杀进程逻辑。

## 修复（commit 662df79）
- 删除 `clearStalePort()` 全部 `taskkill`。
- 新增 `isAnotherInstanceRunning()`：`netstat -ano | findstr :8765` 取 LISTENING PID → `tasklist /FI "PID eq ..."` 确认镜像名含 `Personal AI Dev Center.exe` → 返回 true。
- 顶层 lifecycle：`isAnotherInstanceRunning()` 为真 → `dialog.showErrorBox('已在运行…')` + `app.quit()`；否则保留 `requestSingleInstanceLock()` 兜底。
- `startBackend()` 删除 `clearStalePort()` 调用。

## 验证
- `node --check` 通过；`npm run build` EB_EXIT=0；asar grep 确认新代码进包。
- ⚠️ **沙箱无显示环境，Electron GUI 应用起不来后端**：连用户能启动的解包版在沙箱也 9–12s 退出、无进程无监听，日志仅 `registration_protocol_win.cc CreateFile: 系统找不到指定的文件`。故无法在此端到端验证 GUI 启动；真机逻辑应正常，已请用户在真机双击验证。
- 交付提醒：若真机双击仍提示「已在运行」，先关任务管理器里残留的 `Personal AI Dev Center` / `devcenter-backend` 进程（此前测试可能留孤儿占 8765）。

## 打包
`npm run build` 沙箱 2–3min 成功，未触发 Defender 死锁。

## 知识库扩充（用户需求：最近功能+问题整理成文章）
用户原话：「知识库里的文章太少了，可以把最近我们增加的功能和遇到的问题整理一下，记录一下各种文章」。
存储位置：`%APPDATA%/Personal AI Dev Center/knowledge/<分类>/<id>.md`（前端知识库页实时读取，APPDATA 不入库）。
frontmatter：title/category/tags/author/created/updated，新增文章 author:ai。
本轮新增 6 篇（原 6 → 现 12）：
- 开发文档/同步引擎-ssh连接失败自动重试.md（Task#97）
- 开发文档/仪表盘同步结果摘要面板.md（Task#98）
- 开发文档/区分内容更新时间与同步刷新时间.md（Task#99）
- 架构设计/启动退出进程治理-退出自动关闭后端端口.md
- 运维笔记/便携版启动问题-taskkill自杀根因与只检测提示修复.md（2026-08-29 启动 bug）
- 开发文档/个人资产模块-实体关系与主从建模.md（四类 sim/credit_card/email/membership + 外键关联）
用户重开新打包 exe 后知识库页文章数即从 6 增至 12；无需 git 提交（APPDATA 且 backend/data 被 gitignore）。

## #61 服务器 cron 管理 —— 全栈交付 + 收尾验证（本轮续做）
- 后端：`backend/cron_manager.py`（CronManager + 纯函数校验）；`app.py` 加 4 路由 `/api/cron/{id}/list|get|save|backup`。
  ⚠️ **PyInstaller 关键坑**：app.py 必须是 `from backend.cron_manager import CronManager`（顶层 import），不能写 `from cron_manager import`。`--collect-submodules backend` 只收集 `backend.*`，顶层裸 import 会打包后 `ModuleNotFoundError`（已踩坑修复）。
- 前端：`index.html` 新增「计划任务」按钮 + `openServerCron/loadServerCron/cronHTML/saveCron/backupCron`，只读列举 cron.hourly/daily/weekly/monthly。
- 文档：`knowledge/开发文档/服务器计划任务-cron管理.md`（11 节，用户要求写详细）。
- 单测：`backend/test_cron_manager.py` 7 项全过（validate/list/get/save成功/非法拦截/回滚/无sudo拒绝）。
- 构建：`backend/dist/devcenter-backend.exe` 重建（21.3MB，Sep4 23:47），`npm run build` 输出临时改 `dist_new` 避开实时 app 锁，完成后 `package.json` output 还原 `dist`。
- 收尾验证（本轮）：源 exe 与 `dist_new/.../devcenter-backend.exe` 字节数(21323063)/mtime 完全一致且均含 `cron_manager`；exe 冒烟 `Application startup complete.`（无 ModuleNotFoundError）；`dist_new/index.html` 含 cron UI 字符串。
- 待办：真机端到端 Playwright 验证（连测试机列 root+用户+/etc/cron.d，编辑回写+读回一致+误改回滚）——沙箱无服务器网络，需用户在真机验证。
- 按规矩不主动推 GitHub。
