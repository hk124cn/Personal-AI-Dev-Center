# 2026-08-15 工作日志

## R5 收尾：PyInstaller 内置后端（R1–R10 全部完成）

- 新增 `backend/common_paths.py`：环境变量感知路径解析（`DEV_CENTER_PACKAGED` / `DEV_CENTER_RESOURCE_DIR` / `DEV_CENTER_DATA_DIR` / `DEV_CENTER_APP_VERSION`），统一 dev 与打包两种模式路径行为。
- `backend/` 补 `__init__.py` 成为正式包；`app.py` / `sync.py` 全部改用 common_paths，解决打包后 `from llm_analyzer import` 与 `sys.path` 技巧失效问题。
- `/api/sync` 在 `DEV_CENTER_PACKAGED=1` 时改为**进程内 `import backend.sync` → `sync_all()`**（保留 R8 超时 + `_kill_proc_tree` 语义），彻底摆脱打包后对磁盘 `sync.py` + 系统 `python` 的 subprocess 依赖；dev 模式保留原 subprocess。
- PyInstaller one-file 把 `backend/app.py` 打成自包含 `devcenter-backend.exe`（21MB，含 Python 运行时 + uvicorn/starlette/paramiko/markdown/multipart 等），经 `package.json` 的 `extraResources` 复制进 `resources/backend/`。
- Electron `main.js` 检测到 `resources/backend/devcenter-backend.exe` 存在则 **spawn 该 exe** 并注入四个环境变量（`DATA_DIR` 指向 `%APPDATA%/Personal AI Dev Center/data`，NSIS 安装后 resources 只读）；找不到才回退 `python backend/app.py`。
- 版本号改由 Electron 经 `DEV_CENTER_APP_VERSION` 注入（package.json 移出 extraResources 仅留 asar，避免 asar 校验失败）。新增 `build_backend.bat` 可复现构建。
- 验证：内置 exe 独立冒烟全过（`/api/health`/`/`/`/api/data`=200、`openapi version=1.4.17`、`/api/sync/nonexistent`→404 证明进程内 sync 导入正常）；`npm run build` 成功（96MB）；解包确认 `resources/backend/devcenter-backend.exe` 随包、`app.asar` 含 `package.json`(`main` 入口)+最新 `main.js`、`dist/` 无 `config.json` 及服务器 IP/用户名泄漏、`dist_new` 已清。
- **R1–R10 全部完成**。本地 commit（不 push）。

## README 更新 + 推送到 GitHub
- 中英文 README 更新：版本徽章 1.4.16→1.4.17、简介/架构补「后端 PyInstaller 自包含 exe、目标机免装 Python」、方式一强调双击即用、自行打包补 `build_backend.bat`。commit `2d9c253`。
- 用户明确要推，遂一次性推送 3 个本地提交（`a78b432` 之前的 R8/R9/R50、`a2e04c9` R5、`2d9c253` README）到 `origin/main`（`c775586..2d9c253`）。

## 待办（用户要求暂缓）
- **正式发布 GitHub Release**：把 `dist/Personal-AI-Dev-Center.exe`（96MB，含内置后端、免 Python）作为 Release 资产上传。需用户在 GitHub 后台确认或提供写权限 token。已建任务 #59 跟踪。

## 启动 1Panel 功能模块分析（仅分析，未实现）
- 用户希望复刻 1Panel 部分功能到本系统（多服务器管理增强）。已联网核实 1Panel 模块（应用商店/网站/数据库/容器/文件/主机监控/防火墙/进程/SSH/计划任务/日志审计/AI模型/GPU/备份/告警/多机Agent）与技术栈（Go+Vue3，Core-Agent 架构）。
- 结论方向（详见分析）：监控、文件管理、计划任务(定时同步)、终端 为推荐复刻；防火墙/数据库/容器/应用商店/网站/日志审计 多不适用本系统；本系统差异化优势（资产/SIM/邮箱/会员管理、知识库+LLM、项目同步）保留。实现技术基本可在现有 Python(FastAPI)+Paramiko+Electron 栈内完成，仅「终端」需新增 WebSocket+PTY+xterm.js。

## 1Panel 分析文档 + 实施计划落地
- 已写 `docs/1panel-analysis.md`：含分层判定表、推荐复刻评估、护城河、推进节奏、各功能具体实施计划（主机监控/文件管理/计划任务/终端）。
- **用户重点补充「计划任务」**：不只是定时同步，还要**管理服务器上的 cron**——查看 root 级/用户级(/var/spool/cron/crontabs)/系统级(/etc/crontab、/etc/cron.d、cron.*目录)，区分用户字段，编辑后自动备份+校验+回写服务器+读回验证+失败回滚；非 root 需 sudo（实现前核查 SSH 凭据 sudo 能力）。§5.3.2 已细化。
- 已建任务跟踪：#59 正式发布 Release（暂缓）；#60 主机监控；#62 文件管理；#63 定时同步；#61 服务器 cron 管理（用户重点）；#64 终端(Phase 2)。
- 【#60 主机监控 已完成】用户确认布局方向后落地：
  - 首页概览「服务器状态」面板改为**全宽实时监控区**，「Agent 分布」移至下方独立面板。
  - 新增**环形/进度条一键切换**按钮，配色跟随系统主题（`--accent`）。
  - **进度条视图**复用现有行式布局：左侧服务器信息，中间 3 个进度条（CPU/内存/磁盘）+ 已用/总量数值（如 `1357M/1871M`、`32G/39G`），右侧在线状态 + 运行天数。
  - **环形视图**为 3 列卡片网格，每张卡 3 个小环形仪表（CPU/内存/磁盘），底部负载 + 运行天数。
  - 后端 `/api/monitor/{id}` 已存在并实测可连真实阿里云；前端接入自动刷新（10 秒周期，可暂停/继续）。
  - commit `82c10bd`。
  - 测试时发现 **127.0.0.1:8765 被 PID 8816 顽固占用**（taskkill 失败、tasklist 查不到，疑似孤儿化内置 exe），导致 dev 服务无法绑定 8765；换 8766 验证后端/前端均正常。用户需关闭 Electron app 或重启电脑清掉该进程后再用 8765。
