# 2026-08-11

## 安全/健壮四项修复（R1–R4，v1.4.17 补丁）
按此前系统体检报告，先做了高影响项，并逐项测试确认无回归：
- **R1** 后端 `uvicorn` 绑 `127.0.0.1`（原 `0.0.0.0`）；CORS 收紧为本机来源（原 `*`）。前端 `loadURL('http://localhost:8765')` 同源，不受影响。
- **R2** 上传改流式 `tarfile mode='w|'` → SSH stdin（原 `io.BytesIO` 整包进内存，大项目必崩）。
- **R3** 上传 `tar xf - -C {remote_path}` 加 `shlex.quote`（原仅单引号，命令注入）。
- **R4** 上传加取消：`_UPLOAD_CANCEL` + `/api/sync-upload-cancel/{id}` + 前端 `closeSyncProgress` 对称。

### 测试（用户强调"做完好好测试、别引入新问题"）
- 后端 `py_compile` 语法 OK。
- 集成测试：monkeypatch SSH 层用本机 `tar` 模拟远程解包，真实调用 `sync_upload` → 默认过滤(4)/sync_all(5)/取消(已取消)/空格路径(正确解包) 全过。
- 无头浏览器加载 `http://127.0.0.1:8765`：标题/六大导航/真实数据渲染正常，**0 控制台错误、0 页面错误** → 确认 `index.html` 改动未破坏现有功能，R1 CORS 未挡本机取数。
- 取消端点 `curl` 验证 `/api/sync-upload-cancel` 与 `/api/sync-download-cancel` 均正确注册。

### 提交与状态
- 本地提交 `ede8d8a`（backend/app.py, backend/sync.py, index.html），**未推送**（遵守"仅用户明确要求才推"规则）。
- 用户 live app 仍在跑旧 exe（从 `%TEMP%` 解包，绑 `0.0.0.0`），**未杀**。exe 重建暂缓——重建出的新 exe 也不会被正在跑的旧 app 用到，须待用户关闭 live app 后由我做 `npm run build`（先清孤儿 node.exe 防锁）。
- 用户澄清："换模型"指 WorkBuddy 自身底层模型，与项目无关，不记项目记忆。

## 重建 exe（用户已关闭 live app，23:19 完成）
- 用户确认关闭 live app 后，`npm run build`（electron-builder --win portable）成功，耗时 2m36s，`dist/Personal-AI-Dev-Center.exe` 时间戳 2026-08-11 23:19。
- 构建前确认无 electron-builder 孤儿 node.exe、无 live app 在跑（仅 weixinpay MCP 的 node.exe，无关）。
- **打包验证**（解包 `dist/win-unpacked/resources/` 而非 7z 自解包 exe）：
  - `backend/app.py` 含 `127.0.0.1`(2处)、无 `0.0.0.0`；`sync-upload-cancel`(1) ✅ R1/R4
  - `backend/sync.py` `shlex.quote(remote_path)`(4)、流式 `tarfile.open(fileobj=stdin, mode='w|')`；`io.BytesIO` 仅出现在注释（旧实现说明），非代码 ✅ R2/R3
  - `index.html` 含 `sync-upload-cancel`(1) ✅ R4 前端对称
  - 密钥泄漏扫描：`backend/` + `index.html` 无 sk-/AKIA/私钥前缀 ✅
  - `resources/` 仅含 `app.asar`+`backend/`+`index.html`+`config.example.json`(演示)，**无真实 config.json** ✅
- 注：`build.files=["electron/**/*"]` 只打包 electron/ 进 asar；`backend/`、`index.html`、`config.example.json` 由 `extraResources` 复制到 `resources/`（与 `main.js` 的 `path.join(__dirname,'..','backend','app.py')` 一致）。验证要查 resources/ 而非只 grep asar。
- 用户重启 app 后即生效 127.0.0.1 绑定（R1 全网暴露消除）。
