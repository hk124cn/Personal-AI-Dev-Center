# 2026-08-02 工作日志

## 资产模块增强（个人资产 / SIM卡 / 信用卡）v1.4.14
- 需求：SIM卡在一览显示总额；信用卡同样显示总额；预付卡显示卡内余额；SIM 记录注册网站（点击打开）；SIM/信用卡都加备注且一览显示至少一行。
- 后端 `backend/app.py` 的 `AssetModel` 新增字段：`prepaid`(bool)、`prepaid_balance`(float)、`registrations`(List[dict]，[{name,url}])。资产存于 %APPDATA%/Personal AI Dev Center/config.json 的 `assets` 数组（/api/assets CRUD）。
- 前端 `index.html`：
  - `viewAssets()` 顶部新增 `assetSummaryHTML()` 统计卡：SIM 总费用合计、信用卡总授信额度合计、预付卡总余额合计（按汇率折算 ¥，toCNY+fmtMoney）。
  - `assetRowHTML()`：SIM 卡显示可点击注册网站 chip（`openReg`→window.open）；预付卡右侧显示余额；SIM/信用卡均显示备注第一行（li-note，超长省略号）。
  - 编辑弹窗：SIM 加「注册网站」动态增删列表（data-reg-name/data-reg-url，collectAssetRegs 读取）；信用卡加「是否预付卡」开关 +「卡内余额」；备注改为独立多行文本框。
  - 新增 CSS：.asset-summary/.summary-card/.li-note/.li-regs/.reg-chip。
  - i18n 新增 asset_prepaid/asset_balance/asset_registrations/asset_reg_name/asset_reg_url/asset_add_reg/asset_sim_total/asset_card_limit_total/asset_prepaid_total。
- 校验：node --check 通过；TestClient 验证 AssetModel 正确解析 prepaid/prepaid_balance/registrations 并写入 config（/api/assets POST 持久化成功）。
- 版本 1.4.13 → 1.4.14（package.json + index.html APP_VERSION；app.py/main.js 自动读 package.json）。
- 构建：杀残留进程后 `npm run build`。首次构建在 packaging 阶段假死 32 分钟（环境/electron-builder 偶发，与代码无关），杀掉进程后重建 2.5 分钟完成。`building target=portable file=dist\Personal-AI-Dev-Center.exe` 已成功；末尾 `[safe-delete]` 清理 nsis.7z 报错属已知无害（与 1.4.13 同）。
- 验证（dist/Personal-AI-Dev-Center.exe，19:44，75MB）：resources/index.html 含 1.4.14 与新资产逻辑(assetSummaryHTML/openReg/prepaid 共 18 处匹配)；resources/backend/app.py 含 prepaid_balance；resources/ 仅 config.example.json（无真实密钥）；%APPDATA% config 的 agents 未受影响。

## 资产模块再加「邮箱」类别 v1.4.15
- 需求（用户追加，必须三项）：邮箱名、注册时间、该邮箱申请的网站。
- 后端 `AssetModel` 新增 `email`(str 邮箱地址/邮箱名)、`register_date`(str 注册时间)；复用既有 `registrations`(List[dict] [{name,url}]) 存「申请的网站」。
- 前端 `index.html`：
  - i18n 新增 asset_cat_email/asset_email/asset_register_date。
  - `viewAssets()` cats 加 email 项（图标 mail）；`assetSummaryHTML()` 加邮箱统计卡（数量）。
  - `assetRowHTML()`：email 分支副标题显示「邮箱地址 · 注册于 注册时间」；申请的网站 chip 显示条件由仅 sim 扩展为 `sim||email`；email 不显示费用/到期列（rightCol 置空）。
  - 编辑弹窗：分类下拉加 email 选项；新增 email 分支（邮箱地址、注册时间、申请的网站动态增删区，复用 regList/addRegRow）；总费用行对 email 也隐藏。
  - `saveAsset()` 读取 email/register_date。
- 校验：node --check 通过。
- 版本 1.4.14 → 1.4.15；构建成功（杀残留进程后 npm run build，2m16s，无 safe-delete 报错）。
- 验证（dist/Personal-AI-Dev-Center.exe，21:04，75MB）：resources/index.html 含 1.4.15 与 email 逻辑(asset_cat_email/asset_email/asset_register_date/category==='email' 共 15 处匹配)；resources/backend/app.py 含 register_date；resources/ 仅 config.example.json（无真实密钥）；%APPDATA% config 的 agents 未受影响。
